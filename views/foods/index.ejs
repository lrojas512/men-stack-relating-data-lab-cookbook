<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>My Pantry</h1>
    <a href="/users/<%= user._id %>/foods/new">Add New Food Item</a>

    <% if (pantry.length === 0) { %>
        <p>There is no food in the pantry</p>

   <% } else { %>
    <ul>
        <% pantry.forEach(food => {%>
            <li>
            <%= food.name %> - <%= food.calories %> - <%= food.category %>
            <a href="/users/<%= user._id %>/foods/<%= food._id %>/edit">Edit</a>
            <form action="/users/<%= user._id %>/foods/<%= food._id %>?_method=DELETE" method="POST" style="display:inline">
                <button type="submit">Delete</button>
            </form>
            </li>
       <%  }); %>
    </ul>
   <%} %>
    
</body>
</html>